/**
 * Application fronted main config file
 *
 * @module config
 * @type {object}
 */
define([], function() {
    'use strict';

    var config = {

        // backend api
        api: {

        },
        xhr: {
            timeout: 2000
        }

    };

    return config;
});